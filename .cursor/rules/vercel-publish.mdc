---
description: 用户说「发布」时执行 Vercel 生产环境部署
alwaysApply: true
---

# 触发词：发布 → Vercel 生产部署

当用户在对话中使用 **「发布」** 一词（表示要把当前站点推到线上）时，在**项目根目录** `comic/` 下执行：

```bash
npx vercel deploy --prod
```

## 说明

- 需已登录 Vercel CLI（`vercel login`）；若命令提示交互，把终端输出告知用户处理。
- 该命令会发起网络请求，执行工具时需申请 **network** 权限。
- 若用户刚改过 `bak.html` 且尚未确认生成 `index.html`，可先提醒：是否要先压缩生成 `index.html` 再发布；若用户仅说「发布」且无未同步改动，直接执行部署即可。
